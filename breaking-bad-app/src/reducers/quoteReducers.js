
const initialState = [
  {
    quote: 'I am the Breaking Bad quote app.',
    author: 'Dan Hauer'
  }
];

const reducer = (state = initialState, action) => {
  switch(action.type) {
    default: 
      return state;
  }
}

export default reducer;